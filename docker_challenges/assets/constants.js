/**
 * Configuration constants for Docker challenge frontend.
 *
 * These values control polling intervals and other configurable
 * behavior in the challenge interface.
 */

// Container status polling interval (in milliseconds)
export const CONTAINER_POLL_INTERVAL_MS = 30000; // 30 seconds

// Time conversion constant
export const MS_PER_SECOND = 1000; // Milliseconds per second
